var app = app || {};

(function() {
    'use strict';

    const BASE_URL = '#/',
        LOGIN_URL = '#/login',
        REGISTER_URL = `#/register`;

    app.appUrls = {
        BASE_URL,
        LOGIN_URL,
        REGISTER_URL
    };
}());