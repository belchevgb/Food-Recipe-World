'use strict';

const BASE_URL = '#/',
    LOGIN_URL = '#/login',
    REGISTER_URL = '#/register',
    FRIENDS_URL = '#/friends',
    FOUND_USERS_URL = '#/found-users',
    PROFILE_URL = '#/profile';

let appUrls = {
    BASE_URL,
    LOGIN_URL,
    REGISTER_URL,
    FRIENDS_URL,
    FOUND_USERS_URL
};
export {appUrls as appUrls};